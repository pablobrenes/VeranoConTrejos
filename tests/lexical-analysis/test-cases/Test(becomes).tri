! Test of becomes with 3 unis
:=:=:=

! Test of becomes with array
array :=
array:=
array:=array
array := := := array

! Test of becomes with begin
begin :=
begin:=
begin:=begin
begin := := := begin

! Test of becomes with const
const :=
const:=
const:=const
const := := := const

! Test of becomes with do
do :=
do:=
do:=do
do := := := do

! Test of becomes with else
else :=
else:=
else:=else
else := := := else

! Test of becomes with end
end :=
end:=
end:=end
end := := := end

! Test of becomes with func
func :=
func:=
func:=func
func := := := func

! Test of becomes with if
if :=
if:=
if:=if
if := := := if

! Test of becomes with in
in :=
in:=
in:=in
in := := := in

! Test of becomes with let
let :=
let:=
let:=let
let := := := let

! Test of becomes with of
of :=
of:=
of:=of
of := := := of

! Test of becomes with proc
proc :=
proc:=
proc:=proc
proc := := := proc

! Test of becomes with record
record :=
record:=
record:=record
record := := := record

! Test of becomes with then
then :=
then:=
then:=then
then := := := then

! Test of becomes with type
type :=
type:=
type:=type
type := := := type

! Test of becomes with var
var :=
var:=
var:=var
var := := := var

! Test of becomes with while
while :=
while:=
while:=while
while := := := while

! Test of becomes with .
. :=
.:=
.:=.
. := := := .

! Test of becomes with colon
: :=
::=
::=:
: := := := :

! Test of becomes with ;
; :=
;:=
;:=;
; := := := ;

! Test of becomes with ,
, :=
,:=
,:=,
, := := := ,

! Test of becomes with becomes
:= :=
:=:=
:=:=:=
:= := := := :=

! Test of becomes with ~
~ :=
~:=
~:=~
~ := := := ~

! Test of becomes with (
( :=
(:=
(:=(
( := := := (

! Test of becomes with )
) :=
):=
):=)
) := := := )

! Test of becomes with [
[ :=
[:=
[:=[
[ := := := [

! Test of becomes with ]
] :=
]:=
]:=]
] := := := ]

! Test of becomes with {
{ :=
{:=
{:={
{ := := := {

! Test of becomes with }
} :=
}:=
}:=}
} := := := }

! Test of becomes with +
+ :=
+:=
+:=+
+ := := := +

! Test of becomes with -
- :=
-:=
-:=-
- := := := -

! Test of becomes with asterisk
* :=
*:=
*:=*
* := := := *

! Test of becomes with backSlash
/ :=
/:=
/:=/
/ := := := /

! Test of becomes with =
= :=
=:=
=:==
= := := := =

! Test of becomes with lessThan
< :=
<:=
<:=<
< := := := <

! Test of becomes with moreThan
> :=
>:=
>:=>
> := := := >

! Test of becomes with slash
\ :=
\:=
\:=\
\ := := := \

! Test of becomes with &
& :=
&:=
&:=&
& := := := &

! Test of becomes with @
@ :=
@:=
@:=@
@ := := := @

! Test of becomes with %
% :=
%:=
%:=%
% := := := %

! Test of becomes with ^
^ :=
^:=
^:=^
^ := := := ^

! Test of becomes with questionMark
? :=
?:=
?:=?
? := := := ?

! Test of becomes with CharLiteral
'e' :=
'U':=
'U':='Q'
'$' := := := 'Y'

! Test of becomes with IntLiteral
7567217529843511303 :=
9477975361:=
192475582342433664:=88470239925828227702
6811364354 := := := 69589282807463

! Test of becomes with Indentifier
b9DJk7C5W2E :=
mp0vW2k0Mj6pl5:=
NQf5O1e4S5jyM:=ncz6sicNHg1KECjvAw
RxcW4U5WGT8Q0 := := := LuzButdwVDZ

